# D-009 re-read evidence — elevenlabs

URL: https://elevenlabs.io/pricing
Date: 2026-08-10
Method: mcp__Claude_Browser__navigate + mcp__Claude_Browser__javascript_tool (live render, this
session). Representation 1 re-checked against the already-saved elevenlabs-sources/pricing.html.

What this shows: confirms headline_price_usd=6.00, headline_billing_basis=
per_month_billed_monthly, first_charge_amount_usd=6.00, entry_tier_name=Starter. No change from
the coded record. Not arithmetic-flagged (6.00 x 1 = 6.00, basis is monthly not annual).

## Representation 1 — embedded SSR/FAQPage text (re-grepped fresh today from the saved file)

```
Free $0 (10,000 credits); Starter $6 (30,000 credits); Creator $22 (121,000 credits, $11 for the
first month); Pro $99 (600,000 credits); Scale $299 (1,8...
```

## Representation 2 — live DOM read, javascript_tool, origin=https://elevenlabs.io

Billing control value: `"monthly"` (matches coded default_display_state).

Only `$6` leaf node on the page:
```html
<div class="tw-flex tw-items-end tw-gap-1">
  <span class="tw-type-2xl">$6</span>
  <span class="tw-opacity-80 tw-type-xs">per month</span>
</div>
```
Containing card text starts "Starter$6per month" -- single unified span, no adjacent superscript/
cents node, no duplicate elsewhere on the page.

## Verdict

Both representations agree with each other and with the coded values; today's live figure matches
the saved copy exactly (no final-day change). CONFIRMED, no correction.
