# D-009 re-read evidence — faceless-so

URL: https://faceless.so/pricing
Date: 2026-08-10
Method: mcp__Claude_Browser__navigate + mcp__Claude_Browser__javascript_tool (live render, this
session, Turkey egress). Second representation cross-checked against the already-saved embedded
JS payload at faceless-so-sources/chunks/584c5016af948b8b.js (re-grepped fresh today, not just
trusted from the original coder_note).

What this shows: confirms headline_price_usd=24.00, headline_billing_basis=
per_month_billed_annually, first_charge_amount_usd=290.00, entry_tier_name=Starter. No change
from the coded record.

## Representation 1 — live DOM query, javascript_tool, origin=https://faceless.so

Toggle state (leaf-node aria attributes, not text-extractor order):
```json
{
  "toggleInfo": [
    { "text": "Monthly", "ariaSelected": "false" },
    { "text": "Yearly",  "ariaSelected": "true"  }
  ]
}
```

Price leaf nodes (exact-text match on `^\$\d+$`, deduped across repeated DOM instances of the
same card at different breakpoints):
```
$24/mo  (Starter)
$32/mo  (Growth)
$57/mo  (Influencer)
$82/mo  (Ultra)
```

Billed-yearly caption (leaf-node exact text, deduped):
```
"Billed $290 yearly"
```
Only one match returned for the $24 card -- confirms it is not duplicated/shared with another
row.

Plan-name leaf nodes, in DOM order:
```
Starter, Growth, Influencer, Ultra
```

## Representation 2 — embedded JSON payload (fresh grep, 2026-08-10)

File: faceless-so-sources/chunks/584c5016af948b8b.js (already-saved local copy from original
collection; re-grepped today rather than re-trusted)

```
starterSubscription:{monthly:{name:"Starter (monthly)",price:29,...},
  annual:{name:"Starter (annual)",price:24,pricePerYear:290,...}}
```

## Cross-check

Today's live DOM figures ($24/mo, $290/yr, Starter, Yearly-default) match the saved chunk's
figures exactly and match the original 2026-08-06 collection exactly -- no final-day change.

## Arithmetic flag disposition

check_price_arithmetic.py flags 24.00 x 12 = 288.00 vs first_charge 290.00 (gap $2, ratio 1.007).
Both representations independently agree the vendor STATES $290/yr directly (not derived by the
coder via 12x) and the $24/mo figure is the vendor's own rounded monthly-equivalent display
(290/12 = 24.1667 -> displayed as $24). This is the "documents state the annual total directly"
case in codebook headline_price_usd rule 2 / first_charge_amount_usd rule 2, not a misread.

## Attempted, not obtained

A screenshot (visual third check) was attempted twice. Both times the Browser pane returned a
stale/narrow viewport (276x317) not reflecting the live desktop-resized page, and a subsequent
javascript_tool call on the same tabId showed the tab had navigated to a different origin
(web.archive.org) between calls -- this session's Browser pane tabs are shared with other
concurrent agents in this worktree. Not pursued further per the two-attempt cap; not needed since
the DOM-query and embedded-JSON representations already independently agree.
