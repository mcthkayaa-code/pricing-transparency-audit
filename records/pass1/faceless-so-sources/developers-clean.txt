Developers | Faceless.so Skip to main content Developers Agents Pricing Projects Sign Up / Login Faceless for developers Build with Faceless Turn scripts into finished faceless videos and publish them to YouTube, TikTok, Instagram, X, Facebook, LinkedIn and Threads from your own code. One API key, a REST API, a CLI and MCP servers for agents. Create an API key Read the guides Base URL: https://faceless.so/api/v1 Your first request GET /v1/me curl -s https://faceless.so/api/v1/me \ -H &quot;Authorization: Bearer fl_live_...&quot; API reference Every endpoint, parameter and example in an interactive explorer, generated from the same spec the API runs on. /developers/reference → Guides Ten hand-written guides, from creating a key to rendering, publishing, scheduling and handling errors. Start with getting started → CLI npm install -g faceless-cli. The whole API as commands, with --json output and agent-friendly exit codes. npmjs.com/package/faceless-cli → MCP Remote (streamable HTTP) and local (stdio) MCP servers so agent runtimes call Faceless as tools. Setup guide → Claude Skill A packaged skill that teaches Claude the CLI workflows: create, render, publish, monitor. github.com/Side-Products/faceless-skill → llms.txt Machine-readable index of the site and API surface, made for agents and crawlers. /llms.txt → Guides Getting started From an API key to a published video, with curl and the CLI. Authentication API keys, headers, scopes and key management. Videos The full lifecycle: create, poll, render, download, publish. Series Automated series that generate and post episodes on a schedule. Publishing and scheduling Publish now, schedule for later, cancel, and read the calendar. Assets Register hosted media by URL for use in your projects. Credits and limits What videos cost, plan limits and per-key rate limits. Errors and rate limits The error envelope, every error type and how to react. CLI The faceless command for humans, scripts and agents. MCP Remote and local MCP servers for agent runtimes.