# D-009 re-read evidence — google-veo

Date: 2026-08-10
Method: live browser (Source B) + curl of a specific web.archive.org snapshot (Source A, since
both the live session and the interactive browser tool could not reach USD figures / archive.org).

What this shows: the coded `conflicting` value on headline_price_usd, headline_billing_basis, and
first_charge_amount_usd is CONFIRMED, not a misread on either side. entry_tier_name="Google AI
Plus" CONFIRMED. No source directory existed for this product before this re-read; created now.

## Source A — https://one.google.com/about/plans

Live session: still geo-redirects Turkey egress to TRY (`₺359,99/ay` for "Google AI Plus (2 TB)"),
same as original collection. Archive.org fallback used, matching the original record's method.

`mcp__Claude_Browser__navigate` to web.archive.org was refused this session (as it was for
godaddy earlier in this pass); `curl` reached the exact cited snapshot fine:
```
curl https://web.archive.org/web/20260807100413/https://one.google.com/about/plans -> HTTP 200
```

Representation 1 — embedded schema.org JSON-LD:
```json
{"@type": "Offer", "price": "9.99", "priceCurrency": "USD", ...}
```
(paired via surrounding context with sku g1.2tb)

Representation 2 — data attribute on the plan element:
```html
data-formatted-price="$9.99 / month" data-sku-id="g1.2tb" data-quota-bytes="2199023255552"
```

Representation 3 (bonus) — rendered span:
```html
...(2 TB)</div><div class="F173Pc"><div class="BQxAac "><span>$9.99/mo</span></div></div>
```

All three agree: **$9.99/mo, USD**, for the plan labeled "Google AI Plus (2 TB)".

## Source B — https://one.google.com/intl/en_us/about/google-ai-plans/

Reachable live directly (explicit US-locale path, no redirect).

Representation 1 — live rendered text:
```
Google AI Plus1
$4.99/mo
400 GB storage
```

Representation 2 — raw HTML around the number:
```html
<div class="planCardPrice_mKmrs priceText_iIhJ7 size:headline-5_Bj_8n markdown_rDRZP">
  <g1-localized-price variant="PRICE_GEN_AI_PLUS_MONTHLY" country="US">$4.99</g1-localized-price>/mo
</div>
```
A semantic custom element with its own `variant`/`country` attributes -- unambiguous.

Confirms: **$4.99/mo, USD**, for the plan labeled "Google AI Plus1".

## New observation (not in the original record)

The two pages also disagree on STORAGE for this plan, beyond price: Source A's $9.99 card is
labeled "(2 TB)"; Source B's $4.99 card states "400 GB storage". This is a further divergence
between the two official pages, which supports rather than undermines the `conflicting`
determination -- these are not two readings of the same fact landing on slightly different
numbers, they are two official pages stating substantively different terms for a plan of the same
name.

## Verdict

Both figures reproduce exactly today, each confirmed in 2-3 independent representations. Neither
is a misread. `conflicting` CONFIRMED on headline_price_usd and first_charge_amount_usd.
headline_billing_basis=per_month_billed_monthly CONFIRMED uncontested (both pages quote a
monthly figure). entry_tier_name="Google AI Plus" CONFIRMED (both pages use that base name). No
correction.
