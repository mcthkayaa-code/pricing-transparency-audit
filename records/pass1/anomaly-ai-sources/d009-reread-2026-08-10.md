SOURCE EVIDENCE FILE — D-009 re-read verification
URL: https://www.findanomaly.ai/pricing
Accessed: 2026-08-10
Method: live browser render (mcp__Claude_Browser navigate + javascript_tool body-text search +
JSON-LD structured-data search + screenshot), independent of the 2026-08-06 original-collection
evidence saved in this directory (pricing-raw.html, pricing-text-lines.txt).

--- Representation A: live DOM text search ---

`document.body.innerText` search around "Pro" reads: "...Get Started Free\nMOST POPULAR\nPro\n
$25\n/month\n\nFor recurring analysis, dashboards, and scheduled work\n\n1,000 credits/month...
Start Pro Plan\nAnalyst\n$90..." — matches entry_tier_name (Pro), headline_price_usd (25.00),
headline_billing_basis (per_month_billed_monthly) exactly.

--- Representation B: JSON-LD structured data (schema.org Offer) ---

A fresh full-HTML search found (this re-read's own search, not a repeat of the original coder's
description, which cited a different-looking "page data model" JSON that this re-read could not
locate verbatim -- see note below): `{"@type":"Offer","url":"https://www.findanomaly.ai/pricing",
"priceCurrency":"USD","price":"25","availability":"https://schema.org/InStock","name":"Pro",
"description":"1,000 credits per month — about 600 insights with charts or 200 dashboards"}` --
independent, machine-readable confirmation of Pro = $25.00 USD. (Note: the original record's
evidence field describes the JSON as `"name":"Pro","price":"$25","period":"/month"`, a slightly
different shape/quoting than the schema.org Offer block this re-read found; both agree on the
substance -- Pro, $25 -- and schema.org JSON-LD is itself a form of embedded JSON payload, so
this is treated as satisfying the second-representation requirement even though it is not
byte-identical to the original coder's citation.)

--- Representation C: screenshot (visual, desktop viewport 1440x1000) ---

Screenshot shows "Monthly" toggle with a solid dark active fill versus plain "Yearly Save 20%"
beside it. Three cards visible: Free $0/month, Pro $25/month ("MOST POPULAR" badge), Analyst
$90/month. Pro is the cheapest PAID card (Free is $0, excluded as a paid-tier candidate) --
matches entry_tier_name and the original coder's candidate comparison; no duplicated-row or
mis-mapped-tier risk observed.

--- Provenance check (per coordinator's D-009 addendum) ---

first_charge_amount_usd = 25.00 is NOT computed: the record's own evidence states "No annual
prepayment applies in this default state, so first charge = the displayed monthly figure. No
arithmetic performed (directly read, not calculated)." Confirmed consistent: Monthly is the
default state (screenshot), so no 12x annualization applies. Clean provenance, no issue.

--- Conclusion ---

All four D-009-scope variables confirmed in three representations (DOM text, JSON-LD structured
data, screenshot), matching the 2026-08-06 original reading exactly. No correction.
