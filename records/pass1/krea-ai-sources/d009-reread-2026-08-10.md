SOURCE EVIDENCE FILE — D-009 re-read verification
URL: https://www.krea.ai/pricing
Accessed: 2026-08-10
Method: live browser render (mcp__Claude_Browser, preview + javascript_tool + screenshot), independent
of and in addition to the 2026-08-09 original-collection evidence files in this same directory.
What this shows: re-verification of the four D-009-scope variables (entry_tier_name,
headline_price_usd, headline_billing_basis, first_charge_amount_usd) in two further
representations, per the D-009 sweep method.

--- Representation A: fresh fetch of the page's own embedded JS data payload ---

`fetch('https://www.krea.ai/pricing')` (same-origin, executed in-browser 2026-08-10) returned the
SvelteKit hydration payload containing, verbatim (unquoted JS object literal form, matches the
2026-08-09 pricing-embedded-json-excerpt-2026-08-09.txt byte-for-byte on the relevant fields):

  plansAndPrices.creator_basic = {
    monthly: {amount:9, currency:"usd"},
    yearly: {amount:63, currency:"usd", coupon:"S7vd5tcX"},
    yearlyDiscountPercent: 40, tier:"creator_basic", basePlanId:"creator_basic",
    originalMonthly:{amount:9,currency:"usd"}, originalYearly:{amount:84,currency:"usd"}
  }

This independently confirms first_charge_amount_usd = 63.00 (the yearly.amount field) and that
63 / 12 = 5.25 exactly — the true monthly-equivalent rate. Located at byte offset ~1,656,017 of a
fresh fetch (unquoted `amount:63` form); NOT present in quoted `"amount":63` JSON-LD form and NOT
present in the post-hydration live DOM as text — it exists only in the raw SSR payload embedded in
the initial script bundle, consistent with the original coder's finding.

--- Representation B: screenshot of the rendered price card (default page state, no interaction) ---

Toggle state confirmed via DOM before the screenshot: `document.querySelectorAll('[aria-checked]')`
returned three controls — Monthly (aria-checked="false"), Yearly (aria-checked="true"), and a
second "Annual" switch aria-label="Use annual billing" (aria-checked="true"). Default state is
Yearly/annual, matching default_display_state and annual_default_toggle as coded.

Screenshot (1600x1131 viewport, desktop) of the plan grid shows four cards left to right: Free,
Basic, Pro ("Most popular" badge), Max. The Basic card reads:
  "5.000 units /month" / "64 Nano Banana 2 generations" / "20 Seedance 2.0 videos"
  "$9" (struck through) "$5" "/month"
  button "Get Basic"
No cents or decimal point appears anywhere in the on-screen price digit for any card (Free $0,
Basic $5, Pro $21, Max $63 at its default 60k slider position). This matches the plan-grid row
mapping in the existing record exactly: Basic is the second card, distinct from Free ($0, first
card) and Pro ($21, third card, "Most popular" badge) — no duplicated-row or mis-mapped-tier risk
of the kind D-009 names (the pika error).

--- Conclusion ---

Both new representations reproduce the original coder's reading without exception:
- The vendor's own served data states the true annual charge is $63.00 (=$5.25/mo exactly).
- The vendor's own rendered price card shows the rounded digit "$5" (no cents) as the most
  prominent on-screen figure for the Basic tier under its default (Yearly) toggle state.
The `check_price_arithmetic.py` flag (5.00 x 12 = 60.00 vs first charge 63.00) is the vendor's own
rounding of a displayed monthly-equivalent digit, not a reading error: this is the "legitimate
vendor rounding" case the screen's own docstring anticipates, not a misread of the kind found in
nicepage/pixlr/pika. No value in this record is corrected as a result of this re-read.

--- ADDENDUM 2026-08-10 (second pass): coordinator flagged an apparent self-contradiction ---

The coordinator separately flagged that this record's `computation_assumptions` field reads
"$63/yr=$5.25/mo per page JSON (displayed digit shows $5/mo, see headline_price_usd)" while
`headline_price_usd` is coded 5.0, asking which is right and naming this "likely the real
misread the arithmetic screen flagged." Re-examined with the most rigorous representation
attempted anywhere in this sweep: a raw shadow-DOM inspection of the Basic card's price
component (`number-flow-svelte`, the same element screenshotted above), reading its internal
digit-slot structure directly rather than any text serialization of it.

Result: `sectionInfo` for the four internal sections of this component are, in DOM order:
(1) currency symbol section, symbol="$"; (2) integer section, one digit slot with
`style="--current: 5"` and the single non-inert (i.e., actually displayed) digit node reads
"5"; (3) fraction section: `<span part="fraction" class="section section--justify-left "></span>`
-- a completely EMPTY element, zero child nodes of any kind, not merely zero VISIBLE digits;
(4) suffix section, symbol="/month". The component's own template provisions a fraction slot
(the same markup pattern used elsewhere on this page for prices that DO carry cents, confirming
the component supports rendering a fraction when given one), but for this specific price Krea's
frontend passed it no fraction value at all. This is conclusive: there is no hidden, tiny, or
superscript ".25" anywhere in the rendered output for this price -- not a text-extraction miss
of a real value (the nicepage/pixlr/pika failure mode), but a vendor frontend that was handed
an integer (5) rather than a decimal (5.25) to render. A parent-chain walk (6 ancestors, to the
card's own role="button" container) found no aria-label anywhere in today's hydrated DOM either
(the original coder's cited SSR-only aria-label may not survive hydration; immaterial to the
value itself, which is what a real visitor reads).

On the "self-contradiction": read in full rather than in isolation, `computation_assumptions`
already draws exactly the distinction this re-check confirms -- "per page JSON" ($5.25, the
precise mathematical annual-to-monthly equivalent, used for the cost-per-output arithmetic) is
explicitly distinguished from "displayed digit" ($5, with an explicit cross-reference to
headline_price_usd). Both figures are independently correct for what each represents; the field
is reconciling two true numbers, not asserting one where the other belongs. Verdict on
headline_price_usd: CONFIRMED at 5.00, not corrected. This re-examination does not agree that
krea-ai is "the real misread the arithmetic screen flagged" -- the screen's flag on this record
is fully explained as the vendor's own rounded-display-vs-precise-charge gap, documented in
three independent representations now (embedded JS payload, screenshot, and raw shadow-DOM
digit-slot structure), none of which conflict.
