URL: https://app.jobscan.co/plan
Date: 2026-08-10
Method: D-009 re-read. Live rendered browser session (mcp__Claude_Browser), non-essential
cookies declined via Preferences > Decline All. Second representation obtained via
document.querySelectorAll structural DOM query (raw element text/outerHTML), independent of
the document.body.innerText transcript the original 2026-08-09 coding used.

What it shows:
1. All isolated price-figure leaf nodes on the page, queried structurally
   (elements with zero children whose textContent matches /\$\d/):
   [ {tag: H3, cls: "text-[24px] md:text-[28px] font-semibold", text: "$0"},   <- Free
     {tag: H3, cls: "text-[24px] md:text-[28px] font-semibold", text: "$29.98"}, <- Quarterly
     {tag: H3, cls: "text-[24px] md:text-[28px] font-semibold", text: "$49.95"} ]  <- Monthly
   Card order in the DOM/rendered text matches tab order Free / Quarterly / Monthly, so the
   $29.98 figure maps to the Quarterly card, not a shifted row.

2. First-charge figure, located by direct textContent search for "89.95" (not by trusting an
   innerText transcript's line breaks):
   outerHTML of the matching leaf: <div data-v-2ae903d2=""> Billed as $89.95 every 3 months</div>
   parent: <p class="font-bold mb-[12px] min-h-[24px]"><div...>Billed as $89.95 every 3
   months</div></p>
   Plain text node, no markup splitting the dollars from the cents.

3. Superscript/subscript check (the nicepage failure mode, D-009): document.querySelectorAll
   ('sup, sub') returned 0 elements anywhere on the page. No superscript-cents layout exists on
   this page at all, ruling out the specific misread class D-009 was opened to catch.

4. No pricing JSON API call was observed in the network log for this page (prices appear to be
   compiled into the Vue app bundle rather than fetched at runtime); network request list
   reviewed and contains only Google Analytics/GTM beacons, no /api/plan or similar payload.

Conclusion: headline_price_usd ($29.98, Quarterly) and first_charge_amount_usd ($89.95) both
confirmed in a second representation independent of the original innerText read. Values
unchanged.
