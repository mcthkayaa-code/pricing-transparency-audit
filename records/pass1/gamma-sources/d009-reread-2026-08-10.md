# D-009 re-read evidence — gamma

URL: https://gamma.app/pricing (redirects to https://gamma.app/tr/pricing on Turkey egress, same
as original collection)
Date: 2026-08-10
Method: mcp__Claude_Browser__navigate + mcp__Claude_Browser__javascript_tool + one `computer`
click to dismiss a cookie-consent modal blocking the plan cards.

What this shows: confirms headline_price_usd=9.00, headline_billing_basis=per_seat_per_month,
first_charge_amount_usd=108.00, entry_tier_name=Plus. No change from the coded record. Not
arithmetic-flagged (9.00 x 12 = 108.00 exact).

## Obstruction encountered (reproduces the original coder's own note)

First load: `document.body.innerText` was only 2539 chars and contained no plan cards or "$"
figures at all, despite `document.readyState === "complete"` and a 799KB DOM. A screenshot showed
why: a cookie-consent modal ("About our cookies") was covering the page. Dismissed via its close
(X) control (declining non-essential cookies, not clicking ACCEPT) -- after which the plan cards'
text became reachable. This matches this record's own default_display_state note ("Non-essential
cookies declined (banner dismissed via its close control, ACCEPT not clicked)") exactly; not a new
finding, an independent reproduction of the same mechanism today.

## Representation 1 — live innerText (Turkish UI, after dismissing the modal)

```
Plus
Ekstra yapay zeka gucu ve Gamma markasini kaldirmak icin
$9
/ koltuk / ay
$108 koltuk basina, yillik faturalandirilir
```
("$9 / seat / month", "$108 per seat, billed annually")

Other tiers visible in the same pass: $18/$216 (Pro), $90/$1.080 (higher tier, Turkish thousands
separator, consistent with the original record's note about this formatting).

## Representation 2 — raw HTML around the number

```html
<div class="chakra-stack css-1igwmid">
  <p class="chakra-text css-w75py4">$9</p>
  <p class="chakra-text css-1nyfskl">/ koltuk / ay</p>
</div>
```
Single unified text node for "$9" -- no adjacent superscript/cents span.

## Toggle default state (DOM attribute, not extractor order)

```json
[{"text":"Aylik odeme","ariaPressed":"false"},{"text":"Yillik odeme","ariaPressed":"true"}]
```
`aria-pressed="true"` on "Yillik odeme" (Pay annually) confirms the annual default.

## Provenance of first_charge_amount_usd (per orchestrator request)

$108 is READ directly off the card ("$108 per seat, billed annually" is its own printed line
directly beneath the $9 figure) -- not a coder computation. No `computation_assumptions` entry is
needed for this variable; the record's existing evidence text already says this correctly ("Read
directly as published, not computed (it also equals 9 x 12, consistent)").

## Verdict

Both representations agree with each other and with the coded values; today's figures match the
2026-08-09 collection exactly. CONFIRMED, no correction.
